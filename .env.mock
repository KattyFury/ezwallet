# MOCK MODE - only used when running `npm run mock` (Vite --mode mock reads this file).
# Turns on a fake wallet + fake balances, skips Login/PIN, blocks Circle. NOT a secret, and NEVER in a production build.
VITE_MOCK=1
